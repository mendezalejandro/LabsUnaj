import { HabilitarTurnoDirective } from './habilitar-turno.directive';

describe('HabilitarTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new HabilitarTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
